# Gate A validation scripts must keep LF endings so shebangs work on Linux CI /
# Netlify build images even when checked out on Windows (core.autocrlf=true).
*.sh   text eol=lf
*.js   text eol=lf
*.json text eol=lf
